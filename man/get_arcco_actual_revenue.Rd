% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{get_arcco_actual_revenue}
\alias{get_arcco_actual_revenue}
\title{Get ARC-CO Actual Revenue}
\usage{
get_arcco_actual_revenue(
  crop,
  program_year,
  mya_price,
  nmlr,
  crop_type = NULL,
  yield_type = NULL,
  state = NULL,
  county = NULL,
  fips = NULL,
  quiet = FALSE
)
}
\arguments{
\item{crop}{Character. The crop name to filter for (e.g., "corn", "soybeans").}

\item{program_year}{Numeric. The program year to filter for (e.g., 2024).}

\item{mya_price}{Numeric. The Marketing Year Average price.}

\item{nmlr}{Numeric. The national marketing loan rate.}

\item{crop_type}{Character, optional. The crop type to filter for (e.g., "long grain", "large").}

\item{yield_type}{Character, optional. The yield type to filter for from the yield_type column.}

\item{state}{Character, optional. State identifier that can be:
\\itemize{
\\item State abbreviation (e.g., "IA", "IL")
\\item Full state name (e.g., "Iowa", "Illinois")
\\item State FIPS code (e.g., "19", "17")
}}

\item{county}{Character, optional. County name (requires state to also be specified).}

\item{fips}{Character, optional. 5-digit FIPS code for specific county-level data.}

\item{quiet}{Logical. If TRUE, suppresses warning messages (default: FALSE).}
}
\value{
Numeric. The calculated ARC-CO actual revenue (actual_yield × max(mya_price, nmlr)).
}
\description{
This internal helper function calculates ARC-CO actual revenue by retrieving actual yield
from the fsaArcCoBenchmarks dataset and multiplying it by the higher of MYA price or
national marketing loan rate (NMLR).
}
\details{
The function follows this calculation:
\\enumerate{
\\item Retrieves actual_yield from fsaArcCoBenchmarks dataset
\\item If MYA price >= NMLR: revenue = actual_yield × mya_price
\\item If MYA price < NMLR: revenue = actual_yield × nmlr
}

Location filtering follows the same priority order as other ARC-CO functions.
}
\examples{
\dontrun{
# County-specific actual revenue
get_arcco_actual_revenue("corn", 2024, mya_price = 4.50, nmlr = 4.30, fips = "19001")

# State average with crop and yield types
get_arcco_actual_revenue("rice", 2024, mya_price = 15.20, nmlr = 14.50,
                         crop_type = "long grain", yield_type = "irrigated", state = "IA")
}

}
\keyword{internal}
